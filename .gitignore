# Build artifacts handed off as zips
*.zip

# Dependencies
node_modules/

# Local env
.env
.env.local

# Claude GUI-verification tools (installed by the SessionStart hook)

# Claude GUI-verification tools (regenerated by the SessionStart hook)
.claude/tools/
